let NOISE;_54b‍.x([["getNOISEScore",()=>getNOISEScore],["setNOISEScore",()=>setNOISEScore],["getNOISEAlarm",()=>getNOISEAlarm],["setNOISEAlarm",()=>setNOISEAlarm]]);_54b‍.w("../public/javascripts/defined",[["NOISE",["NOISE"],function(v){NOISE=v}]]);

var IndexTable = require("../models/IndexTable");
var AlarmTable = require("../models/AlarmTable");
var global = require("../global");

       const getNOISEScore = () => {
  console.log("getNOISEScore 호출됨.");

  IndexTable.getIndexTableBySensorType(NOISE, function(err, sensorIndexInfo) {
    if (err) {
      _54b‍.g.console.error(
        "getIndexTableBySensorType 처리 중 오류 발생 :" + err.stack
      );
      return;
    }

    //결과 객체 있으면 성공 응답 전송
    if (sensorIndexInfo) {
      global.sensorTable.noise = sensorIndexInfo;
    } else {
      console.error("NOISE : sensorIndexInfo 정보없음");
    }
  });
};

       const setNOISEScore = (grade, min, max) => {
  _54b‍.g.console.log("setScore 호출됨 : " + grade + "," + min + "," + max);
  IndexTable.updateIndexTable(NOISE, grade, min, max, function(
    err,
    sensorIndexInfo
  ) {
    if (err) {
      _54b‍.g.console.error("updateIndexTable 처리 중 오류 발생 :" + err.stack);
      return;
    }

    if (sensorIndexInfo) {
      global.sensorTable.noise[grade - 1].min = min;
      global.sensorTable.noise[grade - 1].max = max;
    } else {
      console.error("NOISE : sensorIndexInfo 정보없음");
    }
  });
};

       const getNOISEAlarm = () => {
  console.log("getNOISEAlarm 호출됨.");

  AlarmTable.getAlarmBySensorType(NOISE, function(err, sensorAlarmInfo) {
    if (err) {
      _54b‍.g.console.error("getAlarmBySensorType 처리 중 오류 발생 :" + err.stack);
      return;
    }

    //결과 객체 있으면 성공 응답 전송
    if (sensorAlarmInfo) {
      global.alarm.noise = sensorAlarmInfo;
    } else {
      console.error("NOISE : sensorAlarmInfo 정보없음");
    }
  });
};

       const setNOISEAlarm = value => {
  _54b‍.g.console.log("setScore 호출됨 : " + value);
  AlarmTable.updateAlarmValue(NOISE, value, function(err, sensorAlarmInfo) {
    if (err) {
      _54b‍.g.console.error("updateAlarmTable 처리 중 오류 발생 :" + err.stack);
      return;
    }

    if (sensorAlarmInfo) {
      global.alarm.noise = value;
    } else {
      console.error("noise : sensorAlarmInfo 정보없음");
    }
  });
};
